// Store the current year in a variable
var currentYear = 2023;

// Assume the year of birth is 1990 (you can replace it with any other year)
var yearOfBirth = 2000;

// Calculate the age
var age = currentYear - yearOfBirth;

// Print the calculated age
console.log("Age: " + age);